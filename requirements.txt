# Pure-Python cryptography stack (no C build required).
# Pinned exactly to the API these modules are written against.
DoubleRatchet==1.3.0
X3DH==1.3.0
cryptography>=41.0.0
PyNaCl>=1.5.0
stem>=1.8.2
qrcode>=7.4
